# Claude Code's local session config and project instructions -- kept local,
# not pushed to the shared repo.
.claude/
CLAUDE.md

# Supabase CLI's local link state (project ref, pooler connection string) --
# generated by `supabase link`, machine-local, not shared repo state.
fitcoach_backend/supabase/.temp/

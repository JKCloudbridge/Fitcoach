# Local `supabase functions serve` only -- when deployed, Supabase injects
# SUPABASE_URL and SUPABASE_SERVICE_ROLE_KEY into every Edge Function
# automatically, no manual `supabase secrets set` needed for these two.
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
